Place your signed Android APK here as:  wadding.apk

The public download page (/download) links to /downloads/wadding.apk by default.
To use a hosted URL instead, set NEXT_PUBLIC_APK_URL in .env.local.
You can also set NEXT_PUBLIC_APK_VERSION and NEXT_PUBLIC_APK_SIZE.
